# Python requirements for vending machine controller
# Install with: pip3 install -r requirements.txt

# Hardware libraries (required on Raspberry Pi)
RPi.GPIO>=0.7.0  # GPIO control for Raspberry Pi
pyserial>=3.5    # Serial port communication

# Customer display system (required for production)
# NOTE: Pinned to specific compatible versions for Python 3.7+ on Raspberry Pi
flask>=2.0.0,<3.0.0           # Web server for customer display
python-engineio==4.12.3       # Engine.IO server (required by python-socketio)
python-socketio==5.12.0       # WebSocket support (must be >=5.12.0 for flask-socketio 5.5+)
flask-socketio==5.5.1         # Real-time updates via WebSocket

# Optional: Testing framework (not required for production)
# pytest>=7.0.0  # Uncomment if you want to use pytest for testing
