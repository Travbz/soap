# Python requirements for vending machine controller
# Install with: pip3 install -r requirements.txt

# Hardware libraries (required on Raspberry Pi)
rpi-lgpio>=0.4   # GPIO control (drop-in RPi.GPIO replacement for newer Pi OS)
pyserial>=3.5    # Serial port communication

# Customer display system (required for production)
# NOTE: Pinned to specific compatible versions tested on Python 3.7 / Raspbian Buster
flask>=2.0.0,<3.0.0           # Web server for customer display
python-engineio==4.9.0        # Engine.IO server (required by python-socketio)
python-socketio==5.11.0       # WebSocket support (compatible with flask-socketio 5.3.6)
flask-socketio==5.3.6         # Real-time updates via WebSocket
eventlet>=0.30.0              # Async networking for WebSocket support (required by flask-socketio)
simple-websocket>=0.5.0       # WebSocket implementation

# Optional: Testing framework (not required for production)
# pytest>=7.0.0  # Uncomment if you want to use pytest for testing
